class Const {
  // if you change the value on runtime. make sure to restart the app. not to hot reload it.
  static final double cornerWidth = 40;
  static final double cornerHeight = 40;
  static final double cellHeight = 60;
  static final double splitHeight = 25;
}